<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ixuea.courses.mymusic">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/login_netease_selected"
        android:label="@string/app_name"
        android:roundIcon="@drawable/login_netease_selected"
        android:supportsRtl="true"
        android:theme="@style/SuperUI.NoActionBar">

        <activity
            android:name=".component.splash.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 启动界面 -->

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".component.guide.activity.GuideActivity"
            android:exported="false" />
    </application>

</manifest>